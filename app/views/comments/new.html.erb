<h1>New comment!</h1>
<%= form_for(@comment, :url => {:controller => 'comments', :action => 'create', :task_id => @task.id}) do |f| %>
<%= render(:partial => 'form', :locals => {:f => f}) %>
<% end -%>
<%= link_to 'Back', comments_path %>
