<div id="task">
	
	<div id="top">
	
		<h2>
			<%= link_to 'Home', projects_path %> &gt;
			<%= link_to  @task.project.name, project_path(@task.project) %> &gt;
			<%= @task.name %> :
			<%= link_to(image_tag('actions/edit_btn_16.png', {:title => "Edit Task"}), edit_task_path(@task) ) %>
		</h2>
		
		<div class="description">
			<%= @task.description %>
		</div>
		
		<p id="notice">
			<%= notice %>
		</p>
	
	</div>
	
	
	
	<div id="main-content">
		
		<div id="activity-list" class="left">
			
			<p>
				<%= link_to 'Log Time', new_time_log_path(:task_id => @task.id), :class => 'button'%> 
				<%= link_to 'Post Comment', new_comment_path(:task_id => @task.id), :class => 'button'%>
			</p>



			<% recent_date = @task.activity.first.created_at.to_date  if @task.activity.first %>
			
			<% classArr = ["", "right_of_line"] %>
			<% toggle = 0 %>
			<h2 class="day"><%= recent_date.strftime("%A %d %B") %></h2>
			<div class="seperator">
			<% @task.activity.each do |a| %>
			
				<% if a.created_at.to_date < recent_date %>
					</div>
					<h2 class="day"><%= a.created_at.strftime("%A %d %B") %></h2>					
					<div class="seperator">
					<% recent_date = a.created_at.to_date %>
				<% end %>
				
				<% toggle == 0 ? toggle = 1 : toggle = 0 %>
				<% if a.instance_of?(Comment)  %>
				
				<%= render :partial => 'activity_comment', :locals => { :comment => a, :my_class => classArr[toggle] } %>
				<% elsif a.instance_of?(TimeLog)  %>					
				<%= render :partial => 'activity_time_log', :locals => { :time_log => a, :my_class => classArr[toggle]   } %>
				<% end %>			
			<% end %>
			</div>
			


			<!-- thing-->
		</div>
		
		<div id="metrics" class="left">
			<div class="polabox">
				<p>Deadline: <%= @task.deadline.strftime("%A %d %B %Y") %></p>
				<p>Days til deadline: <%= @task.days_left %></p>
				<p>User: <%= image_tag(@task.user.avatar.url(:teeny), {:title => @task.user.name}) %> <%= @task.user.name %></p>
				<p>Days Allocated: <%= @task.work_days %></p>
				<p>Days Spent: <%= @task.work_days_spent %></p>
				<p>Percentage of Time used: <%= @task.work_percent_spent %></p>
			</div>
			
		</div>
		
	</div>
	
	
</div>



<div class="clear"> </div>

<%= link_to 'Edit', edit_task_path(@task) %> |
<%= link_to 'Back', project_path(@task.project_id) %>
