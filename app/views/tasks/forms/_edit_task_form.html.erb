<div id="edit-task-form" class="form">	
	<h2>Edit Task</h2>	
	<%= form_for(@task, :url => {:action => 'update', :id => @task.id}) do |f| %>
	<%= render(:partial => 'tasks/forms/form', :locals => {:f => f}) %>
	<% end %>
</div>