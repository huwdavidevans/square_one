<%= render(:partial => 'form_errors', :locals => {:object => @task})%>	



  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.select(:user_id, User.all.collect {|r| ["#{r.first_name} #{r.last_name} : (#{r.username})" , r.id]}) %></td>
	</div>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :deadline %><br />
    <%= f.date_select :deadline %>
  </div>
  
   <div class="field">
  	<%= f.label :supplied_work_days, "Days worth of work"  	%>
  	<%= f.select(:supplied_work_days, (1..10).to_a  ) 	%>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
