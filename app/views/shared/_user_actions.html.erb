<%= link_to :controller=>'user', :action=>'show', :id=>current_user.id do %><%= image_tag current_user.avatar.url(:tiny) %> <%= current_user.name %><% end -%><%= link_to("log out", :controller => 'access', :action=>'logout') if session[:user_id]  %>